# harbour

Cool blue-grey, hairline rules and tight radii. Neutral, professional, dense.

## Tokens

`[data-palette='harbour']` in `resources/css/themes.css`, which is the one
place they are written. Every colour is a `light-dark()` pair, so this palette
is declared once and answers both schemes — which half applies is the *other*
axis and is not this file's business. See [README.md](README.md).

## Mood

Neutral, professional, dense.

## Pairs with

- console — best
- stacked — best
- workspace — best
- focus — good
- marketing — good
