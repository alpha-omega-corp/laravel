# vellum

Monochrome, square corners, and one red for emphasis. Austere, luxury, typographic.

## Tokens

`[data-palette='vellum']` in `resources/css/themes.css`, which is the one
place they are written. Every colour is a `light-dark()` pair, so this palette
is declared once and answers both schemes — which half applies is the *other*
axis and is not this file's business. See [README.md](README.md).

## Mood

Austere, luxury, typographic.

## Pairs with

- focus — best
- marketing — best
- console — good
- stacked — good
- workspace — fair
