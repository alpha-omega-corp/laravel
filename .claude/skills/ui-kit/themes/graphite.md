# graphite

Near-black and blue with a mono display face. Technical, instrument-panel.

## Tokens

`[data-palette='graphite']` in `resources/css/themes.css`, which is the one
place they are written. Every colour is a `light-dark()` pair, so this palette
is declared once and answers both schemes — which half applies is the *other*
axis and is not this file's business. See [README.md](README.md).

## Mood

Technical, instrument-panel.

## Pairs with

- console — best
- workspace — best
- stacked — good
- focus — fair
- marketing — fair

## Costs

A mono display face and an instrument-panel palette. In `marketing` it reads as a
developer tool, which is right for a developer tool and wrong for everything else.
